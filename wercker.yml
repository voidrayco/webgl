box: kkarczmarczyk/node-yarn
build:
  steps:
    - script:
      name: Clean up
      code: |
        rm -rf ./node_modules
    - script:
      name: Yarn Install
      code: |
        yarn install
    - script:
        name: Test the build
        code: npm run build
